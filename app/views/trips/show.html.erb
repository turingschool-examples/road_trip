<h1><%= @trip.title %></h1>
<%= @trip.destination_city %>
<%= @trip.mileage %>

<section id="travelers-list">
  <h2>Current Travelers</h2>
    <% @trip.travelers.each do |traveler| %>
      <section id="traveler-<%= traveler.id %>">
        <%= traveler.name %>
        <%= link_to "Remove Traveler", "/trips/#{@trip.id}/#{traveler.id}", method: :delete %>
      </section>
    <% end %>
</section>
