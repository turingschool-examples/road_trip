<h1>All Trips</h1>

<section id = "trips">
  <ul>
    <% @trips.each do |trip| %>
      <li><%= link_to "#{trip.title} (#{trip.mileage} miles)", trip_path(trip) %></li>
    <% end %>
  </ul>
</section>